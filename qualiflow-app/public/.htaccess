RewriteEngine On
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [L]

# Sicurezza base demo: disabilita directory listing se il server lo supporta.
Options -Indexes
